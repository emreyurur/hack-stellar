name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  # ─── Smart contract tests ───────────────────────────────────────────────────
  contract-tests:
    name: Smart Contract Tests (Soroban)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: contracts/counter

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust stable
        uses: dtolnay/rust-toolchain@stable

      - name: Cache Rust build artifacts
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: contracts/counter

      - name: Run counter contract tests
        run: cargo test -p hello-world --lib -- --nocapture

      - name: Run pool-router contract tests
        run: cargo test -p pool-router --lib -- --nocapture

  # ─── Frontend ───────────────────────────────────────────────────────────────
  frontend:
    name: Frontend (Build + Tests)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Type check
        run: npx tsc --noEmit

      - name: Lint
        run: npm run lint

      - name: Run unit tests
        run: npm run test

      - name: Build production bundle
        run: npm run build
